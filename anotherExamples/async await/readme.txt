For run those files, follow next steps:

1.- Install "node-fetch" package using "nodejs command prompt" writting next command:
	>$ npm install node-fetch --save
2.- For run any of the index.js files, write next command:
	>$ node index
	
	or
	
	> node index2

It's important not to write the "js" exention in the last command